initialization-data
initializeNamed: aName with: aColor of: aMass in: aPosition with: aVelocity and: anAcceleration

	name := aName.
	color := aColor.
	mass := aMass.
	position := aPosition.
	velocity := aVelocity.
	acceleration := anAcceleration