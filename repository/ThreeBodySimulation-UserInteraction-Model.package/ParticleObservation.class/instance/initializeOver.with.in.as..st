initialization
initializeOver: observedParticleState with: particleColor in: containerUniverseObservation as: initialMode

	particleState := observedParticleState.
	color := particleColor.
	universeObservation := containerUniverseObservation.
	observationMode := initialMode