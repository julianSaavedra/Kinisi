initialization
initializeBasedOn: particleDraft size: shapeSize using: factor

	color := particleDraft color.
	positionFactor := factor.
	size := shapeSize.
	labelIsEnabled := false.
	shouldRenderVelocity := false.
	shouldRenderPositionMarks := false
	